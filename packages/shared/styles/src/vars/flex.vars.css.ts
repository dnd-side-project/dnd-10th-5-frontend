import { createVar } from '@vanilla-extract/css';

const alignItems = createVar();

const flexBasis = createVar();

const flexDirection = createVar();

const flexGrow = createVar();

const flexShrink = createVar();

const flexWrap = createVar();

const justifyContent = createVar();

export const flexStyleVars = {
  alignItems,
  flexBasis,
  flexDirection,
  flexGrow,
  flexShrink,
  flexWrap,
  justifyContent,
};
