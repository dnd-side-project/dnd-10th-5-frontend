import { globalVars } from '@favolink-ui/react';
import { style } from '@vanilla-extract/css';

export const header = style([
  {
    color: globalVars.palette.archiveCoral,
    backgroundColor: globalVars.palette.archiveYellow,
  },
]);
